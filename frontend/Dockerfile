# Frontend is deployed on Vercel — not Docker.
# See https://vercel.com for deployment instructions.
# Build command:  npm run build
# Output dir:     .output/public  (or dist)
# Framework:      Vite
# Env var:        VITE_GRAPHQL_URL=https://your-gateway.onrender.com/graphql
